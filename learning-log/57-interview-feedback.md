The service prevents overwriting feedback with outcome !== 'pending'. A recruiter submits the wrong outcome due to a UI bug, realises immediately, and wants to correct it. The system blocks the correction. Propose a design that allows corrections within a short time window (e.g., 5 minutes after submission) while preventing retroactive changes. What database changes are needed, and how does the time window affect the stage management logic?

To allow corrections without enabling retroactive changes, add a `feedback_created_at` timestamp to the interviews table and enforce a 5-minute correction window in the service logic—if feedback already exists and the elapsed time exceeds 5 minutes, reject the update; otherwise allow the correction and log the change for audit purposes. This approach maintains data integrity while giving recruiters a brief grace period to fix genuine mistakes, and it doesn't affect stage management logic beyond ensuring stage transitions triggered by feedback only happen if the feedback is locked (after the window expires).

---

recordInterviewFeedback runs two sequential queries: updateInterviewFeedback then updateApplicationStage. If the process crashes between these two writes, the interview shows outcome = 'moved_forward' but the application stage has not advanced. An operator examining the database finds an inconsistency: the interview outcome contradicts the application stage. Describe a detection query that finds all such inconsistencies, and a safe remediation strategy.

A detection query can find mismatches by joining interviews and applications where the interview outcome indicates progression (`'moved_forward'` or `'hired'`) but the application stage remains earlier in the pipeline (e.g., `'screening'` or `'applied'`), revealing cases where the stage update failed after feedback was recorded. To remediate safely, run an idempotent background job that re-applies the correct stage transition for each inconsistent application—ensuring the stage is moved to the appropriate next step without duplicating work—and log all changes for auditing, effectively resolving the inconsistency without manual intervention.