requireRole throws ForbiddenError even when req.user is undefined — the case where authMiddleware was not wired before it. The error message is "Insufficient role", which is not quite accurate: there is no role to be insufficient, because there is no authenticated user at all. A developer debugging a mis-wired route chain might spend time looking for an auth token problem before realizing authMiddleware is simply missing. What would you change about the error handling in requireRole to make the mis-wiring case easier to diagnose in development — without leaking any implementation detail in production?

I will add a check for unidentified user in authmiddleware and update it to null than reqRole can check if its !user then return else continue, or we could simply check for unidentified user in requirerole and return a more specific response to that

The two-middleware pattern — authMiddleware then requireRole — means every role-gated route lists both explicitly. In a route file with ten protected routes, authMiddleware appears ten times. A developer suggests applying authMiddleware once at the router level (router.use(authMiddleware)) so individual routes only need to list requireRole. This works for routes where every endpoint is protected. What does it break when some routes on the same router are public?

this would lead to requiring viewer to be a registered user to view the content, so it wont be really be public a better way could be to use nested routes and the routes that require these middlewares, there parents can include this middlewares instead of repeating.